/**
 * ثوابت التطبيق المركزية
 * يتم استيراد هذه القيم بدلاً من استخدام أرقام مباشرة (Magic Numbers) في الكود
 */

module.exports = {
    // ===== أمان الحساب =====
    /** عدد المحاولات المسموحة قبل قفل الحساب */
    MAX_FAILED_ATTEMPTS: 5,
    /** مدة قفل الحساب بالدقائق */
    LOCKOUT_DURATION_MINUTES: 15,

    // ===== رموز التحقق =====
    /** صلاحية رمز التحقق بالدقائق */
    VERIFICATION_CODE_EXPIRY_MINUTES: 10,
    /** عدد أرقام رمز التحقق */
    VERIFICATION_CODE_LENGTH: 6,

    // ===== الصفحات والحدود =====
    /** عدد العناصر الافتراضي في الصفحة */
    DEFAULT_PAGE_LIMIT: 50,
    /** الحد الأقصى لعدد السجلات في استعلام واحد */
    MAX_RECORDS_LIMIT: 1000,
    /** الحد الأقصى لعدد العناصر في الإضافة المتعددة */
    MAX_BULK_ITEMS: 50,

    // ===== طلبات الحذف =====
    /** مدة صلاحية طلب الحذف بالساعات */
    DELETION_REQUEST_EXPIRY_HOURS: 24,
};
